# You can use this file to create a conda environment for this pipeline:
#   conda env create -f environment.yml
name: nf-core-diaproteomics-1.0dev
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - r-base=3.6.1
  - conda-forge::r-rmarkdown=1.16
  - openms=2.4.0
  - python=2.7.15
  - msproteomicstools=0.8.0
