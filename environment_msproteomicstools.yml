# You can use this file to create a conda environment for this pipeline:
#   conda env create -f environment.yml
name: nf-core-diaproteomics-1.0_msproteomicstools
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=2.7.15
  - msproteomicstools=0.8.0
  - statsmodels=0.10.1
